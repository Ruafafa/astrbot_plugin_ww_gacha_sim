{
  "enable_rendering": {
    "description": "是否开启渲染功能",
    "type": "bool",
    "hint": "启用或禁用抽卡结果的图片渲染功能",
    "default": true
  },
    "enable_history_recording": {
    "description": "是否记录历史抽卡记录",
    "type": "bool",
    "hint": "是否保存用户的抽卡历史记录",
    "default": true
  },
    "save_rendered_results": {
    "description": "是否保存渲染结果",
    "type": "bool",
    "hint": "是否将渲染的抽卡结果图片保存到指定文件夹",
    "default": false
  },
    "render_output_path": {
    "description": "渲染结果保存路径",
    "type": "string",
    "hint": "抽卡结果图片的保存目录路径",
    "default": "./rendered_results"
  },
  "config_storage_path": {
    "description": "卡池配置文件存储路径",
    "type": "string",
    "hint": "卡池配置文件的存储目录路径",
    "default": "./card_pool_configs"
  },
  "cache_cleanup_interval": {
    "description": "资源缓存清理周期",
    "type": "int",
    "hint": "资源缓存自动清理的时间间隔（单位：小时）",
    "default": 24,
    "minimum": 1,
    "maximum": 720
  },
  "enable_proxy": {
    "description": "是否开启网络代理",
    "type": "bool",
    "hint": "是否启用网络代理功能，开启后需配置有效的代理地址",
    "default": false
  },
  "proxy_url": {
    "description": "网络代理地址",
    "type": "string",
    "hint": "网络代理地址 (例如 http://127.0.0.1:7890), 如果省略协议头默认使用 http://",
    "default": ""
  }
}